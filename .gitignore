node_modules/
.env
.env.local
.DS_Store
dist/
coverage/
*.log
data/cache/labmaus/
data/cache/pokepaste/
data/cache/pikalytics/
data/cache/vgcguide/
data/cache/metavgc/

# Production runtime DB — accumulates captured tournament/team/team_set rows
# from labmaus + pokepaste + pikalytics ingest. Build script regenerates the
# category-A roster portion non-destructively. NEVER track this file in git;
# `git checkout` would silently wipe production state. See memory
# `single_db_non_destructive_build.md` (2026-05-06 incident).
data/reg-m-a/db.sqlite

# Seeded-vector test fixture for VGC-T46. The binary should be regenerated by
# a generator script that encodes the deterministic ranking invariants in
# reviewable code (TODO(stage6-deferred): scripts/generate-seeded-vectors.ts).
# Tracking the binary in git makes it indistinguishable from a hand-edited
# blob and offers no diff signal at code review.
fixtures/knowledge/seeded-vectors/vectors.bin
