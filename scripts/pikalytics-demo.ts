/**
 * Stage 4 stub for the pikalytics operator demo script (per flow §6 Q9).
 * Real implementation lands in Stage 5.
 *
 * Mirrors `scripts/labmaus-latest.ts` shape — prints, for a hard-coded
 * species (defaults to `sneasler`), the top-N Pikalytics teammates so an
 * operator can sanity-check the data after an ingest run.
 */

throw new Error("not implemented (Stage 5): scripts/pikalytics-demo");
