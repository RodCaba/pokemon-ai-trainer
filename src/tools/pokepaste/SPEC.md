# pokepaste tool — SPEC (placeholder)

Full spec lands in Stage 5 per `docs/plans/pokepaste-sets.md` §4.3. This file
exists so the Stage 4 stubs compile and so the tool layer mirrors the
labmaus precedent (`src/tools/labmaus/SPEC.md`).

TODO(stage 5): inputs, outputs, edge cases, cache + throttle policy, error
matrix, citation rules, Reg M-A hygiene clauses, reject-and-fail validation
contract.
